<?xml version="1.0"?>


<globals>

    <!-- Путь для ресурсов -->
    <global id="resOut" value="${resDir}" />

    <!-- Путь для .java/.kt файлов -->
    <global id="srcOut" value="${srcDir}/${slashedPackageName(packageName)}" />

</globals>
